spring:
  application:
    name: matchservice
  datasource:
    url: jdbc:h2:mem:matchdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
